/**
 * A TypeScript interface.
 * @interface AccountModelIFace is model Database return to account model.
 */

export interface AccountModelIFace {
  id: string;
  name: string;
  email: string;
  password: string;
}
